apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app-1-b
  namespace: app-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-app-1-b
  template:
    metadata:
      labels:
        app: web-app-1-b
    spec:
      containers:
      - name: nginx
        image: nginx:1.24.0
        ports:
        - containerPort: 80
